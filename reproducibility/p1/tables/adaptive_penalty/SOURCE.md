# Source: adaptive_penalty

Manuscript: Appendix F (penalty adaptation with a common residual) and the
median/rho-change summary.

Study: the adaptive-penalty comparison. `adaptive_penalty.csv` is the delivered
table (21 scenes x 6 rules: grid reference / fixed structure / He mean / He
structure / normalized mean / normalized structure), regenerated by
`regenerate.py` from the stored per-run JSONs beside it.

- Status: `regenerated` (from stored measurements).
- Rerunning the physics is NOT possible from this directory (internal solver);
  the stored `solution_sha256` pairs are the bit-identity evidence (126/126).
- Regenerate here: `python regenerate.py`.
